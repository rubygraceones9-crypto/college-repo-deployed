sonar.projectKey=rubygraceones9-crypto_college-evaluation-system
sonar.organization=rubygraceones9-crypto

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=college-evaluation-system
#sonar.projectVersion=1.0

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# Exclude specific files from duplication and general check
sonar.exclusions=node_modules/**,.next/**,build/**,dist/**,public/**,coverage/**,**/*.test.tsx,**/*.test.ts,**/*.spec.tsx,**/*.spec.ts,tools/**,scripts/**,data/**,fix-sonar*.js
sonar.cpd.exclusions=app/teacher/profile/page.tsx,app/dean/profile/page.tsx,app/api/audit/route.ts,app/api/evaluations/dean/route.ts,app/api/academic_periods/route.ts,app/api/courses/route.ts,**/*.js,**/*.ts,**/*.tsx

# Exclude coverage if it fails
sonar.coverage.exclusions=**/*
